services:
  dialogue-agent-mcp:
    build: .
    env_file: .env
    restart: unless-stopped

  test:
    build:
      context: .
      dockerfile: Dockerfile.test
    env_file: .env
    profiles: ["test"]
